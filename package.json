{
  "name": "veasna-os",
  "private": true,
  "packageManager": "pnpm@11.15.1",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "setup": "pnpm install && node scripts/setup-env.js",
    "build": "pnpm -r build",
    "dev": "pnpm --filter universe dev",
    "dev:bp": "pnpm --filter bp dev",
    "dev:gamedev": "pnpm --filter loom-engine dev",
    "dev:vstudio": "pnpm --filter vstudio dev",
    "dev:all": "concurrently -k -n universe,bp,gamedev,vstudio -c blue,green,yellow,cyan \"pnpm dev\" \"pnpm dev:bp\" \"pnpm dev:gamedev\" \"pnpm dev:vstudio\"",
    "dev:desktop": "concurrently -k -n next,electron -c blue,magenta \"pnpm dev\" \"wait-on http://127.0.0.1:3000 && pnpm --filter @veasna/desktop dev\"",
    "build:desktop": "pnpm --filter universe build && pnpm --filter @veasna/desktop package",
    "publish:desktop": "pnpm --filter universe build && pnpm --filter @veasna/desktop release"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "wait-on": "^8.0.1"
  }
}
