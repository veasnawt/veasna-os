// Moved to packages/vcut/src/import/mediaFormats.ts so the native (on-device) import path can
// share the exact same extension/kind rules instead of a second hand-maintained copy.
export { SUPPORTED_EXTENSIONS, kindForExtension } from "@veasnawt/vcut/src/import/mediaFormats";
